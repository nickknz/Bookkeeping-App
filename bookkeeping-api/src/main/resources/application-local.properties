# IntelliJ / 本机启动时启用：SPRING_PROFILES_ACTIVE=local
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/bookkeeping}
spring.datasource.username=${DB_USERNAME:bookkeeping}
spring.datasource.password=${DB_PASSWORD:bookkeeping}
