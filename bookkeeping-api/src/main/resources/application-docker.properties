# Docker Compose 内启动时启用；db 是 Compose 服务名。
spring.datasource.url=${DB_URL:jdbc:postgresql://db:5432/bookkeeping}
spring.datasource.username=${DB_USERNAME:bookkeeping}
spring.datasource.password=${DB_PASSWORD:bookkeeping}
